<li class="nav-item">
            <a class="nav-link ps-0 disabled" href="#" aria-current="page"
            onclick="loadData(`gallery`, 'gallery-template', 'gallery'); disableLink(this)">Show all</a>
        </li>
        {{#each data}}     
        <li class="nav-item">
            <a class="nav-link" href="#" onclick="loadData(`gallery/categories/{{id}}`, 'gallery-template', 'gallery'); disableLink(this)">{{name}}</a>
        </li>
        {{/each}}
    </script>
    <script id="gallery-template" type="text/x-handlebars-template">
        {{#each data}}     
        <div class="col-md-3">
            <img class="img-thumbnail mb-3" src="{{thumbpath}}" alt="{{name}}">
        </div>
        {{/each}}